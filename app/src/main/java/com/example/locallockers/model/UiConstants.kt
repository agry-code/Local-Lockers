package com.example.locallockers.model

import androidx.compose.ui.unit.dp
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.sp

// Objeto que contiene constantes de la interfaz de usuario (UI).
object UiConstants {
    val IconSize = 28.dp  // Tamaño de los iconos.
    val ArrowIconSize = 36.dp  // Tamaño de los iconos de flecha.
    val boxSize = 40.dp  // Tamaño de las cajas.
    val fontSizeL = 16.sp  // Tamaño de la fuente grande.
    val fontSizeM = 14.sp  // Tamaño de la fuente mediana.
    val fontSizeS = 10.sp  // Tamaño de la fuente pequeña.
}